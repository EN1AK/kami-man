## 1. riftsim prerequisite (recorded here because `rift-rag` lives in the riftsim repo)

The previous one-shot service (`scripts/rag/rag_query.py` + `scripts/rag/rag_server.py`, contract `{query, top_k} → {answer, warnings, sources}`) stays intact as the fallback and `oneshot` mode. All riftsim work below runs from the riftsim repo root.

- [ ] 1.1 Add `scripts/rag/card_resolver.py`: build the card alias index from `cards_bilingual.db.cards` + R-CARD `topic` strings (card_key / name_cn / name_en / subtitle composites / normalized forms); deterministic resolution (card-id regex + longest-match + quoted spans; exact → normalized → fuzzy selection; ambiguity marking; bounded retry for unresolved); optional constrained LLM extraction/selection gated by `RAG_LLM_CARD_EXTRACTION` / `RAG_LLM_CARD_SELECTION` (default off). Verify by `python -m pytest scripts/rag/tests/test_card_resolver.py -q` covering: exact CN name, subtitle-composite full name, EN name, absent DB (degrade to id matching), quoted span, same-name variant ambiguity, punctuation normalization, fuzzy uniqueness, unresolved after bounded retry.
- [ ] 1.2 Refactor `scripts/rag/rag_query.py` into retrieval tools over an `EvidencePool`: `get_card_rules` (rules ⋈ rule_cards join), `search_rules` (existing hybrid + resolved-card canonical-text expansion, cap 2 cards), `lookup_rule` (id/number). Keep the existing one-shot `retrieve`/`generate` functions working unchanged (fallback + `oneshot` mode). Verify by `python -m pytest scripts/rag/tests/test_retrieval_tools.py -q` including join correctness, expansion behavior, and one-shot parity with the pre-refactor pipeline.
- [ ] 1.3 Add `scripts/rag/agent_loop.py`: planner→tool loop with strict JSON action schema, `RAG_AGENT_MAX_STEPS` (default 4, clamp 1-8), duplicate-signature rejection, 2-strike invalid-output break, `submit_answer` citation validation (answer `[rule_id]` markers and `citations` must be pool members), and deterministic fallback to one-shot generation over the pool (`exhausted: true`). Verify by `python -m pytest scripts/rag/tests/test_agent_loop.py -q` using a fixture LLM (scripted planner): happy path with cited submit, citation-not-in-pool rejection then fixed resubmit, step exhaustion → fallback with `exhausted`, invalid-JSON break, duplicate-call rejection.
- [ ] 1.4 Update `scripts/rag/rag_server.py` for contract v2: accept `mode`/`trace`, run agent loop for `mode=agent`, emit `resolved_cards`/`coverage`/`exhausted`/`mode` (+`trace` when requested), keep validation errors and 404/405/500 semantics unchanged. Verify by rerunning its existing tests plus new v2 cases, and curl-smoking: `POST {"query":"海兽钓钩的放逐怎么处理"}` (agent), same with `"mode":"oneshot"` parity, `"trace":true`, invalid `mode` → 400 `{"error": ...}`.

## 2. Add the Riftbound RAG API client settings and response

- [ ] 2.1 Update `services/rift_rag_api.py`: add `mode` / `trace` to `RiftRagSettings` with envs `RIFT_RAG_MODE` (default `agent`) and `RIFT_RAG_TRACE` (default off); `query_rift_rag()` sends `query`, `top_k` if used, `mode`, `trace`; extend `RiftRagResponse` with tolerant optional fields `resolved_cards` (list of `{card_id, mention?, name_cn?, name_en?, ...}`), `unresolved_mentions` (list[str]), `ambiguous_mentions` (list), and `exhausted` (bool, default False). Verify by rerunning `python -m pytest tests/test_rift_rag_api.py -q` and adding `tests/test_rift_rag_api.py` cases for base URL config, payload containing `query`+`mode`, full-v2 parsing, and legacy `{answer, warnings, sources}`-only parsing.
- [ ] 2.2 Update `services/rift_rag_messages.py` and `services/qa_common.py` for a recognized-coverage write path. Verify by rerunning `python -m pytest tests/test_rift_rag_api.py -q` with `services.rift_rag_qa_base_url()` returning the configured or default base URL.

## 3. Add merged-forward Riftbound RAG message building

- [ ] 3.1 Update `services/rift_rag_messages.py` build data/functions: question node appends `识别卡牌：…` when `resolved_cards` is non-empty, `识别失败：…` / `卡名歧义：…` warning lines when coverage is non-empty, and `已达检索步数上限，答案基于部分证据` when `exhausted`; answer/source node behavior unchanged. Verify by updating `python -m pytest tests/test_rift_rag_messages.py -q` with cases for populated/empty/missing v2 fields.
- [ ] 3.2 Implement `build_rift_rag_nodes()` with question (+coverage lines), cited-rule, and answer nodes in order, sourced-sender metadata, useful line preservation, topic line only for populated topics, and the configured-source footer node. Verify by rerunning `python -m pytest tests/test_rift_rag_messages.py -q` covering question rules, identity behavior, topic omission behavior, and answer/footer node content.

## 4. Wire the group QA plugin

- [ ] 4.1 Keep `plugins/rift_rag_qa.py` group-only alias triggering as-is (`问规则`, `规则`, `问下规则`; `请在命令后写下问题内容` for empty text; private chats get no response). Verify by rerunning `python -m pytest tests/test_rift_rag_qa.py -q -k group_alias_or_trigger`.
- [ ] 4.2 Keep the QA request→forward flow (`query_rift_rag()` + `build_rift_rag_nodes()` + merged-forward send) and the fixed failure mapping (`连接失败：请确认 7862 端口服务已启动` / `请求超时，请稍后重试` / `RAG 服务返回异常 ...` / `RAG 服务返回了空结果 ...`). Ensure `exhausted=True` responses still forward normally (not mapped to failure). Verify by rerunning `python -m pytest tests/test_rift_rag_qa.py -q` covering identity fallback, placeholder finished sources without `rule_id`, plain message fallback only on forward-send failure, source-list filtering for QA paths, and `exhausted` forwarding.

## 5. Verification and archive readiness

- [ ] 5.1 Run `python -m pytest` for the new riftsim rag tests, all repo tests, `python -m py_compile bot.py services/rift_rag_api.py services/rift_rag_messages.py services/qa_common.py plugins/rift_rag_qa.py scripts/rag/card_resolver.py scripts/rag/agent_loop.py scripts/rag/rag_query.py scripts/rag/rag_server.py`, and `openspec validate add-riftbound-rules-rag-qa --strict`, then confirm all pass.

## 6. Deployment

- [ ] 6.1 In the riftsim repo upload `rules.db`, `rules_vectors.npy`, `rules_index.json`, and `cards_bilingual.db` to the server data directory; set/update required envs (`OPENAI_API_KEY`, `RAG_LLM_MODEL`, proxy; optional `RAG_AGENT_MAX_STEPS`, `RAG_LLM_CARD_EXTRACTION`, `RAG_LLM_CARD_SELECTION`, `RAG_CARD_RESOLUTION_TOP_K`) in `/etc/systemd/system/rift-rag.service.d/override.conf` or `/etc/rift-rag/rift-rag.env`. Verify by confirming expected files and env vars exist on the server and the service loads the cards index without warnings.
- [ ] 6.2 Start/restart `rift-rag.service`, confirm both `mode=agent` and `mode=oneshot` return 200, then execute `python bot.py` to start the bot. Verify by confirming the service and bot process are running and the QA endpoint responds for a card question with non-empty `resolved_cards`.
- [ ] 6.3 In a real QQ group, send `问规则 麦田圈在对方回合能发动吗` (pure rules), `问规则 海兽钓钩的放逐怎么处理` (card), and a garbage query, and confirm the question node shows 识别卡牌 for the card question, cited `[R-...]` markers are present in the answer, fixed failure texts appear for failure cases, and private messages get no reply. Verify by confirming replies match the scenarios in `openspec/changes/add-riftbound-rules-rag-qa/specs/riftbound-rules-rag-qa/spec.md`.
- [ ] 6.4 Update `docs/production.md` with the new request/response fields (`mode`, `trace`, `resolved_cards`, `coverage`, `exhausted`), the `cards_bilingual.db` data requirement, and the new service/env flags (`RAG_AGENT_MAX_STEPS`, `RAG_LLM_CARD_EXTRACTION`, `RAG_LLM_CARD_SELECTION`, `RIFT_RAG_MODE`, `RIFT_RAG_TRACE`). Verify by reviewing the updated section and confirming no lingering one-shot-only notes remain.
